#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints Numbers
 *
 * Return: gives 0 as output
 */

int main(void)
{
	int r;

	for (r = 0; r <= 10; r++)
		printf("%d", r);
	printf("\n");
	return (0);
}
